interface:
  display_name: "PR Ready"
  short_description: "Validate changes and open a focused pull request"
  default_prompt: "Use $pr-ready to validate these changes and prepare a new pull request."
